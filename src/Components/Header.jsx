import React from 'react'
import cl from '../App.module.scss';
const Header = () => {
  return (
    <div className={cl.br}>
    <header className={cl.header}>
        <h2 className={cl.headerText}>Where In The World?</h2>

    </header>
    </div>
  )
}

export default Header